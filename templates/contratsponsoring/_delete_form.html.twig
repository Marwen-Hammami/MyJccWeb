<form method="post" action="{{ path('app_contratsponsoring_delete', {'idContrat': contratsponsoring.idContrat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contratsponsoring.idContrat) }}">
	<button>Delete</button>
</form>
