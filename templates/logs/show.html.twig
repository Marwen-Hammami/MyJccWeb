{% extends 'base.html.twig' %}

{% block title %}Logs{% endblock %}

{% block body %}
    <h1>Logs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ log.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_logs_index') }}">back to list</a>

    <a href="{{ path('app_logs_edit', {'id': log.id}) }}">edit</a>

    {{ include('logs/_delete_form.html.twig') }}
{% endblock %}
