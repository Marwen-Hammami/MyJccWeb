<form method="post" action="{{ path('app_planningfilmsalle_delete', {'idPlanning': planningfilmsalle.idPlanning}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planningfilmsalle.idPlanning) }}">
    <button class="btn">Delete</button>
</form>
