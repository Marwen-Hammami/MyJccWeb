<form method="post" action="{{ path('app_prix_delete', {'idPrix': prix.idPrix}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prix.idPrix) }}">
    <button class="btn">Delete</button>
</form>
