{% extends 'base.html.twig' %}

{% block title %}Prix index{% endblock %}

{% block body %}
    <h1>Prix index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdPrix</th>
                <th>Typeprix</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for prix in prixes %}
            <tr>
                <td>{{ prix.idPrix }}</td>
                <td>{{ prix.typeprix }}</td>
                <td>
                    <a href="{{ path('app_prix_show', {'idPrix': prix.idPrix}) }}">show</a>
                    <a href="{{ path('app_prix_edit', {'idPrix': prix.idPrix}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_prix_new') }}">Create new</a>
{% endblock %}
